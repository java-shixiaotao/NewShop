<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yq.dao.LevelDao">
    <select id="list" resultType="com.yq.entity.Level">
        select * from tb_level where level_id=#{level_id}
    </select>
</mapper>